1. install nodejs nodejs.org.
2. open cmd node -v.
3. make any folder and navigate to it using command prompt.
4. hit the command "npm install" to load all the depecnices.
5. hit the command "node app.js"
6. open browser and hit url as "localhost:1735'